spring:
  application:
    name: market
  datasource:
    url: jdbc:postgresql://localhost:5430/market
    username: postgres
    password: OiH56kjlo.
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration (совпадает с Python Gateway)
jwt:
  secret-key: "test-secret-key-1234567890-abcdefghijklmnop"
  algorithm: "HS256"
  access-token-expire-minutes: 30
  refresh-token-expire-days: 7