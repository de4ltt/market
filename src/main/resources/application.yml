spring:
  application:
    name: market
  datasource:
    url: jdbc:postgresql://localhost:5430/market
    username: postgres
    password: OiH56kjlo.
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect